To install an assistant bot, follow these steps:
1) Launch the console
2) Run "pip install colorama"
3) Go to directory: "../Helper3000/bot_helper"
4) Run "pip install ."
5) Done. Now you can run the bot in the console from any directory by running the command "Helper3000"
